input {
  beats {
    port => 5044
  }
}

filter {
  # Optional: add filters if needed (e.g., grok, mutate)
}

output {
  elasticsearch {
    hosts => ["http://localhost:9200"]
    index => "metricbeat-%{+YYYY.MM.dd}"
  }
}
